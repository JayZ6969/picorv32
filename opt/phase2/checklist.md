# Phase 2 Checklist

- [ ] `make formal` passes
- [ ] `make formal_full` passes (when required)
- [ ] `make baseline_capture` generates `opt/results/phase2/pcpi_summary.csv`
- [ ] `make baseline_check` reports PASS
- [ ] Baseline artifact archived with run outputs
